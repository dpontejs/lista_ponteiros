#include <stdio.h>

int main() {
    
    /*
    int i, j, *p, *q

    a) p = &i: pode ser atribuido, ponteiro recebe endereco
    b) *q = &j: nao pode, pois *q espera uma variável do tipo int
    c) p = &*&i: pode, pois p vai receber o endereco de i ao final
    d) i = (*&)j: nao, pois o operador de desrreferenciacao nao tem uma posicao para agir
    e) i = *&j: pode, pois o endereco de j desrreferenciado é um valor int
    f) i = *&*&j: pode, pois o primeiro par *& irá retornar j e o segundo também
    g) q = *p: nao, pois q espera um endereco de memória
    h) i = (*p)++ + *q: pode, pois o resultado será um valor int
    */
   
   return 0;
}